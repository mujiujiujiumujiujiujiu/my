# 梗战争模拟器交接记录

- 阶段：Prototype
- 玩家结果：在预算内买兵并部署，开战后观看多波自动战斗；波次无缝衔接，不再有战后整备或中途操作，清空整关后进入结算并可前往下一关，用剩余预算获得评级。
- 当前状态：原生 WebGL2 可玩切片已接入《How Many Dudes?》式连续波次自动战斗节奏，并按需求收敛为纯排兵布阵流程；10 关、13 个可部署单位、数据驱动兵种、部署/撤回、拖拽、自动战斗、技能、连续波次、镜头、暂停/倍速和结算已经接通。准备阶段支持“兵种卡拖到左半场直接部署”，已部署单位拖回任意兵种卡会撤销并全额退款。
- 移动与辅助：远程/召唤/辅助单位使用数据中的守距、后撤距离与后撤倍率；近战追击远程或辅助目标时使用追击倍率，避免双方移速相近时近战永远摸不到后排。卡面显示生命、攻击、移速和射程/近战。辅助技能统一支持移速、伤害/攻速、减伤等可计算增益；绿色光环、落点脉冲、头顶增益环、短标签和事件播报会反馈效果，治疗技能在满血目标上也可触发增益。
- 移动端入口：手机方向使用 `visualViewport` 宽高识别，按短边与触控能力判断手机类视口；`x > y` 时使用普通横屏布局，`y > x` 时加入 `is-virtual-landscape`，把长边作为游戏宽度、将整个应用舞台旋转 90° 填满视口。Canvas backing size、相机拖拽、点击部署、兵卡拖拽和拖回兵卡撤销都使用同一套旋转后逻辑坐标。入口仍包含 `height=device-height`、横屏 meta、`manifest.webmanifest` 的 `orientation: landscape`；物理全屏/`screen.orientation.lock('landscape')` 只作为支持设备的 best-effort 优化，不再阻塞虚拟横屏。`resize`、`orientationchange`、`fullscreenchange` 和 `visualViewport.resize` 都会重新适配画布与相机。
- 手机性能：首屏不再等待 2 MB PNG 图集；`index.html` 内嵌数据、样式和运行代码，默认图集改为 `./assets/units-handdrawn-atlas-mobile.webp`，约 243 KB，低优先级异步加载。直接双击 `file://` 时，浏览器会拒绝把本地图片上传到 WebGL，运行时会改用 `#worldSprites` 的 DOM 图集裁切回退，保持角色手绘图可见且不再抛 Console 异常；HTTP/GitHub Pages 使用移动 WebP 的正常 WebGL 纹理。原始 PNG 保留为高画质源文件，不进入默认请求；极限低清 WebP 保留为慢网备用。移动 WebGL 使用低功耗、关闭抗锯齿、DPR 上限 1.25、较少圆环细分，并降低世界标签 DOM 更新频率。
- 部署与碰撞：准备阶段按角色可见图幅计算安全半径，点击、兵卡拖入和已部署单位移动都拒绝与其他单位重叠；拖入拥挤位置会停在上一个有效点，拖回兵卡仍会撤销并全额退款。战斗模拟使用多轮分离求解，传送、冲锋、召唤和移动后的角色都会在同一更新周期内保持可见图幅不叠放。
- 手机卡栏：虚拟横屏时卡栏与卡片使用 `touch-action: none`，由输入层按物理纵向距离手动更新逻辑横向 `scrollLeft`；手指留在卡栏内沿滚动轴移动会滚动，离开卡栏或朝战场移动会进入拖兵，避免浏览器把部署拖拽提前 `pointercancel`。普通横屏仍使用 `pan-x` 原生滚动。
- 静态发布：`index.html` 不依赖外部脚本、样式或 JSON，可直接双击或部署到 GitHub Pages；完整仓库需提交 `assets/units-handdrawn-atlas-mobile.webp` 与 `manifest.webmanifest`，高清 PNG 与极限低清 WebP 可选，缺默认图集时才会退回颜色/文字占位。
- 重要文件：`index.html`、`manifest.webmanifest`、`package.json`、`src/main.js`、`src/sim.js`、`src/webgl.js`、`data/units.json`、`data/levels.json`、`assets/units-handdrawn-atlas-mobile.webp`、`assets/units-handdrawn-atlas-low.webp`、`assets/units-handdrawn-atlas.png`、`scripts/check-data.mjs`、`scripts/check-balance.mjs`、`scripts/build-static.mjs`、`scripts/check-static-runtime.mjs`、`scripts/server.mjs`、`scripts/launch.mjs`、`PLAY_GAME.cmd`。
- 验证：`npm run build` 成功；`npm run check` 通过，确认 13 个可部署单位与 10 个关卡数据完整；固定种子基准编队 10/10 关完整清场；远程追击回归确认前排在 15.2 秒追上远程目标；辅助回归确认 4 个技能能施加可计算增益；碰撞回归逐帧确认活动角色不穿过可见图幅；静态检查确认入口无外部脚本/样式、无 base64 图集、默认路径为移动 WebP 且纹理为懒加载相对路径，并守护 `file://` 美术回退层。Playwright 响应式回归确认 `432×804` 触控视口的虚拟横屏 Canvas 逻辑尺寸为 `600×262`，相机缩放 `0.295` 时世界四角全部在 Canvas 内；844×390 横屏仍为战场 640×220；HTTP 下移动 WebP `spriteReady=true`，直接 `file://` 下 DOM 手绘角色回退可见、无 Console error；两种尺寸均可部署并进入自动战斗，竖屏触控滚动卡栏、拖兵与拖回兵卡撤销也已验证。当前生成入口约 157 KB，移动图集约 243 KB，极限低清图集约 112 KB，PNG 高清源约 2 MB。
- 当前风险：所有阵容的公平性仍需真实试玩；严格不叠图约束让第 10 关基准编队可能接近 103 秒，战斗超时上限已从 90 秒放宽到 120 秒；WebGL2 不可用时只有提示，没有 Canvas 2D 降级；浏览器不能保证物理屏幕自动旋转，竖直握持时虚拟横屏会让整个横向舞台旋转显示，真正可读的物理横屏仍建议用户把设备转横；如果提交 GitHub 时漏掉 `assets/units-handdrawn-atlas-mobile.webp`，任何模式都会只能显示占位图；manifest 横屏声明主要对安装到主屏的 PWA 生效；真机 GPU、触控手感、刘海/手势条安全区、GitHub Pages 缓存与带宽尚未在本轮复测。
- 下一项最安全任务：先在一台真实手机上通过 GitHub Pages 分别验证物理横屏与竖直握持下的虚拟横屏可读性、刘海/手势条遮挡和首次可操作时间；再根据真实数据微调单位移速/守距/辅助持续时间，不先扩大技能系统。
