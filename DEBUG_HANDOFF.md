# 调试交接记录

## 本轮错误现象

- 用户直接双击 `index.html` 后，页面一直停留在“正在加载战场数据…”。
- 用户提供的真实 Console：`Access to script at 'file:///.../src/main.js' from origin 'null' has been blocked by CORS policy`，随后 `main.js` 加载失败。

## 最小重现步骤

1. 双击项目根目录的 `index.html`。
2. 等待页面初始化。
3. 预期进入“准备部署”，实际加载遮罩不消失。

## 当前错误讯息 / Log

- 用户本机直接打开时，浏览器明确阻止 `file:///.../src/main.js` 外部脚本，页面 origin 为 `null`。
- 本地 HTTP 入口可进入“准备部署”，且当前控制台无错误。

## 已尝试修法与证据

- 已将 JSON 数据内嵌到 `index.html`，并把模块脚本改为普通脚本；`npm run check` 与本地 HTTP 冒烟通过。
- 已把纹理加载改为非阻塞；`npm run check` 与本地 HTTP 冒烟通过。
- （历史记录）当时 `index.html` 仍通过三个外部 `<script src="file:///...">` 加载运行时代码；用户 Console 证明 Chrome 会在 `file://` 场景阻止它们。当前构建入口已改为内嵌运行时代码，见下方修复结果。

## 失败原因

- 上一版只内嵌了数据，没有内嵌运行时代码；浏览器将外部 `file://` 脚本视为跨源请求并拦截，导致 `main.js` 没有机会执行。

## 根因假设

- 当前根因已由用户日志确认：外部脚本资源与 `file://` 的 `origin: null` 触发浏览器 CORS 限制。

## 下一個可否證驗證步驟

- 让构建脚本把 CSS、运行时代码和图集都内嵌进 `index.html`，再用静态检查确认入口不含外部脚本；本地 HTTP 冒烟确认仍能进入“准备部署”。

## 修复结果

- 已将纹理加载改为后台可选任务；`init()` 不再等待它完成。
- `bootstrap()` 异常现在会隐藏加载遮罩并给出可见失败提示，不再伪装成永久“正在加载”。
- `scripts/build-static.mjs` 现在会将 CSS、运行时代码、JSON 数据和 PNG 图集全部内嵌到 `index.html`，不再依赖被 `file://` CORS 拦截的外部脚本。
- `npm run check` 通过；单文件构建后本地 HTTP 真实浏览器修复后进入“准备部署”，控制台无错误。

## 不准再重复的修法

- 不再把可选图片、音频或其他装饰资源作为隐藏加载遮罩的无超时硬依赖。
- 不再把“普通 `<script src>` 能在 `file://` 下加载”作为直接双击入口的前提。

## 本次验证

- `npm run check`：通过，13 个可部署单位与 10 个关卡数据完整。
- Node 固定种子模拟：覆盖 10 个关卡、敌方全员同时入场、远程后撤、召唤物保持距离，并为每关验证了预算内完整清场基准编队。
- 浏览器 smoke test：WebGL2 成功启动；兵种卡、己方战斗角色、召唤物和敌方瓜摊老板均显示原创手绘图集；无控制台错误。

## 已知风险

- 纹理加载失败时会回退到几何占位，并在界面提示；这不是 WebGL2 不可用时的 Canvas 2D 降级。
- 当前图集是原型级角色表现，动作由程序化 WebGL 特效驱动，后续可拆成逐角色动画帧并增加命中特效音频；手机横屏布局与拖卡/拖回退款已在 844×390 浏览器视口验证，但仍需真实触控设备复测。

## 本轮攻击/受击反馈修复

- `src/sim.js` 新增统一 `beginAction()` 动作状态：近战挥击、远程起手、技能施放和死亡都拥有独立时长与方向；`applyDamage()` 统一生成受击方向、命中点、撞击特效和确定性伤害数字，避免视觉层随机改变固定种子战斗结果。
- `src/main.js` 根据动作状态对图集角色做起手位移、受击抖动/闪白和缩放；绘制延迟血条层、攻击轨迹、命中火花、击退方向线，并将 `floatingTexts` 映射到半透明 DOM 数字。
- `src/main.js` 的 VFX 样式按单位区分 `slash/rhythm/heavy/shield/bark/spark/magic/blink/delivery/penguin/poke/melon`，所以同样是命中不会所有角色都只显示同一种环形效果。
- `styles.css` 将角色名、技能标签和伤害数字改为低不透明度背景，减少对战场和血条的遮挡；兵种卡新增技能摘要。
- `scripts/check-static-runtime.mjs` 增加统一动作/命中反馈链与伤害数字渲染层的存在性检查。

## 本轮角色朝向与技能重构

- `src/main.js` 删除角色本体永久圆环、圆形底座、中心色块和朝向线；部署预览改为低透明度方形轮廓，受伤状态改为命中点短放射线，避免与投射物/技能圆形效果混淆。
- `src/sim.js` 让投射物携带 `sourceId`、`projectileStyle` 和准确的来源单位；蔡徐坤的远程普攻绘制为橙色篮球，第四拍对目标周围造成节奏 AOE 并主动后撤。
- `data/units.json` 为 14 个角色的攻击/技能补充数据化 VFX 与击退/减速持续时间：华强霰弹、嘎子狙击、大运/牛来冲撞、大狗吠叫、耄耋召唤/W 嘴、妙脆角猫瞬移、奶蛙奶泡、黄色袋鼠外卖、企鹅控制、刀盾反伤和瓜摊老板西瓜投掷均保持明显差异；远程/辅助单位使用 preferredDistance 主动拉开距离。
- `scripts/check-static-runtime.mjs` 增加“无角色本体底圈/篮球样式/投射物数据”回归保护；第 9、10 关烟测编队改为适配新的远程技能定位。

## 本轮验证

- `npm run build`：通过，最新瓜摊老板修正版图集与水彩战场底图已内嵌到 `index.html`。
- `npm run check`：通过；数据校验、10 关固定种子平衡烟测、静态自包含检查和动作/命中反馈回归检查均通过。
- 本地 HTTP 浏览器：通过；AX 树确认 13 张兵种卡包含技能摘要，真实战斗截图出现远程弹道、技能播报、伤害数字与血条变化；DOM 读取到 `.world-label` 背景 `rgba(5, 15, 24, 0.42)`；控制台 error/warn 为空。

## 本轮布局与交互回归

- PC 1280×720：战场画布 1000×518，无横向溢出；窗口尺寸变化会重新执行相机适配。
- 手机横屏 844×390：准备态战场 844×206，战斗态战场 844×330；任务栏改为右上角可收缩悬浮层，战斗时底部部署栏为 0 高度且无横向溢出。
- 交互：兵种卡拖到左侧部署区成功；已部署的华强拖回兵种卡后部署数回到 0，并显示“华强 已撤销，500 金已退回”。

## 本轮数值、特效与 UI 回归

- `npm run check`：通过；`check-balance.mjs` 报告 10/10 关基准编队完整清场，通关时间 16.1–38.0 秒。
- 浏览器 1280×720：战场 1000×518，静态入口加载后无控制台 error/warn。
- 浏览器 844×390 横屏：准备态战场 844×206、战斗态战场 844×330；右上角任务栏可收缩，准备态卡栏 844×124，开战后卡栏隐藏且 WebGL 画布重新 fit，文档无横向/纵向溢出。
- 浏览器真实捕获到“敌方全员入场”“护盾”“瞬移”“召唤”上下文标签和对应事件播报；传送落点、冲锋轨迹、召唤生成环和延迟爆发均由 `src/sim.js` 生成、`src/main.js` 绘制。
- UI 审计：Readable 8/10、UX 8/10、Accessibility 7/10、Commercial 9/10、Retention 8/10、Google Play Risk 4/10。真实手机横屏 GPU/触控和安全区尚未复测。

## 继续排查时的最短路径

1. 先运行 `npm run check`。
2. 启动 `npm run start`，打开 `http://127.0.0.1:4173/`。
3. 检查 `assets/ASSET_NOTES.md` 的图集索引与 `data/units.json` 的 `spriteIndex` 是否一致。

## 本轮移动与响应式回归

- 根因：近战单位的推进速度由 70–88 的基础 `spd` 决定，1600×820 世界的接敌距离让低速前排显得拖沓；不能直接全局提高加速度，否则会改变远程后撤和所有技能节奏。
- 修复：`src/sim.js` 通过 `getMoveSpeed()` 读取单位数据的 `moveMultiplier`，只为大运、大狗、牛来、凑企鹅设置近战推进增益；召唤的小橘猫覆盖为 `moveMultiplier: 1`。调参使用固定种子回归，过高倍率会让第 9 关烟测前排脱离保护，因此最终采用 1.12/1.10/1.18/1.12。
- 响应式根因：390×844 下右侧说明栏原有高度 138px 但内容 scrollHeight 196px；并且 `max-width:520px` 的网格规则会渗透到窄横屏。修复为竖屏 154px 两列显式布局，并在 ≤720px 横屏重置为块流，避免标题、提示与战场读数被挤成窄列。
- 触控处理：横屏顶部选择/按钮最小高度改为 44px；窄横屏侧栏宽 204px，底部卡列表高度 67px，`body` 在 720×390 与 844×390 均无溢出。
- 验证命令：`npm run build`、`npm run check`；浏览器视口 390×844、844×390、720×390、1280×720；桌面实战 4× 一次性全阵容胜利；`tab.dev.logs({levels:['error','warn']})` 返回空数组。

## 本轮单场编队、渐进渲染、部署条与纵深排序

- 关卡数据现在只保留 `enemies`。`BattleSimulation` 构建 `enemyRoster` 后一次性创建整场敌军，已删除波次索引、推进器和波次入场事件；四列错位生成让多单位同场更易读。固定种子 `check-balance.mjs` 已验证 10 关基准编队都能在预算内完整清场。
- 新的 VFX 是 `src/main.js` 中的按攻击风格程序化绘制分派，每个角色至少有独立投射物/命中形状或节奏：篮球、奶泡、西瓜、外卖包、W 嘴、爪痕、雷电、节拍、冲撞尘土、刀盾六边形、魔法微粒等；死亡使用淡出/碎片/柔和冲击，避免血腥。
- 质量策略在 `src/webgl.js` 与 `src/main.js`：low/medium/high 控制 DPR、网格和特效预算，首帧先跑低档，浏览器空闲后才尝试高清图集；省流量或慢网络保持低档。风险是目前低档仍使用同一个自包含 HTML 中的高清资源路径，没有单独低清 PNG，所以首包体积仍可继续优化。
- `unitList` 已同时支持桌面鼠标/触控笔拖卡和手机原生上下触摸滚动；卡片到中央战场左半场部署、拖回退款仍沿用原流程。本轮浏览器回归已验证 390×844 自动横屏纵向卡栏、移动端卡片部署、PC 纵向卡栏和控制台 error/warn 为空。
- `drawUnitsAndEffects()` 先按 world Y 升序绘制单位，越靠下（Y 越大）越晚绘制，因此遮挡层级越高；投射物与 VFX 在演员之后绘制。角色名称继续采用半透明标签，不恢复永久圆圈/方向线。
- Steam 参考页：[How Many Dudes?](https://store.steampowered.com/app/3934270/_/)。只借鉴喜剧化模拟战的移动、出手、命中和退场节奏，保持本项目原创素材和温和死亡表达。
- 最短回归命令：`npm run build`、`npm run check`；然后打开本地 HTTP 入口，验证准备态、一次性全员入场、攻击/受击数字、撤回退款、横向卡栏和手机竖/横屏布局。
- 已知边界：真实手机的刘海安全区、触控手感、GPU 帧率与横竖屏旋转仍需实体设备复测；不要把浏览器视口通过当作 Android/iOS 认证。

## 本轮自动逻辑横屏、纵向部署栏与凑企鹅价格回归

- 根因：原先部署栏位于底部，会压缩准备态战场；移动端横向滚动逻辑也不适合“左侧上下滑动”的兵种选择方式。
- 修复：HTML 将任务面板提升为 `#app` 直属右栏，CSS 三栏布局把 `#unitDock` 放左侧、`#playArea` 放中间、`#sidePanel` 放右侧；`unitList` 改为 `flex-direction: column`、`overflow-y: auto`、`touch-action: pan-y`。触控滑动由 `startScrollTop` 和纵向轴判定维护，横向离栏仍可拖卡到战场。
- 自动横屏保持前一轮逻辑：390×844 时应用按 844×390 渲染并 CSS 旋转铺满，不显示横屏提醒；WebGL 使用 client 尺寸，避免旋转后的 physical bounding rect 造成模糊/拉伸。
- 数值：凑企鹅价格改为 20 金，`check-data.mjs` 的最低价格守卫同步为 20；固定种子 10 关平衡烟测仍全部完整清场。
- 验证：1440×900 和 844×390 截图显示左卡栏/中央水彩战场/右战况栏；列表内容高度 1147px、视口高度 258px，滚动回归成功；拖第一张卡部署成功，战斗态部署栏隐藏、中央 backing 为 788×334；390×844 无提示且逻辑尺寸正确；`npm run build`、`npm run check`、浏览器 error/warn 均通过。
- 剩余风险：需真实手机复测页面级旋转的文本朝向、刘海安全区、DPR 和 GPU 帧率；不应把浏览器视口仿真当成实体设备认证。

## 本轮长边横屏与全战场取景回归

- 现象：先前横屏布局把战场固定在右侧说明栏左边，844×390 只有 608px 宽；战斗隐藏部署栏但仍保留底部网格行，造成可视区域浪费和相机/画布尺寸不同步。
- 修复：`src/main.js` 以 `visualViewport` 计算长短边，竖屏手机直接切换自动逻辑横屏渲染，不再显示横屏提示；`ResizeObserver` 与显式 `syncRendererToLayout()` 在网格尺寸变化后同步 WebGL backing canvas 和相机。`styles.css` 改为左侧纵向部署栏、中央战场、右侧任务/战况栏，战斗/结算态移除部署栏并扩大中央画布；移动缩放下限降到 0.22，保证窄屏仍能完整容纳 1600×700 世界。
- 触控回归：卡栏内横向移动仍滚动；触控/鼠标拖动一旦离开卡栏就转为拖兵，844×390 从华强卡拖至左半场后部署数变为 1，开始战斗成功。
- 视觉/性能回归：`src/webgl.js` 中高质量纹理启用 mipmap/各向异性过滤并清理旧纹理，low/medium/high DPR 上限为 1/1.75/2.4；844×390 战斗截图显示左右阵营与世界边界，面板为收缩小条，部署栏不再占位。
- 测试证据：`npm run build`、`npm run check` 通过；1440×900 与 844×390 均验证左侧纵向卡栏、中央战场和右侧 UI，纵向滚动与拖卡部署成功；390×844 无横屏提示且自动使用长边逻辑视口；战斗期间 AX 树没有部署栏，中央画布逻辑 backing 为 788×334；`tab.dev.logs({levels:['error','warn']})` 返回 `[]`。
- 未覆盖：真实设备安全区、DPR>1 的实际 GPU 负载、系统旋转动画和低网速真实下载；移动低清首帧目前是低 DPR/低特效预算，不是独立低分辨率图片资源。
